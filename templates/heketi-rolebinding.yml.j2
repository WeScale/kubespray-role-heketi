---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "{{ heketi_name }}"
  namespace: "{{ heketi_namespace }}"
subjects:
  - kind: ServiceAccount
    name: {{ heketi_name }}
    namespace: {{ heketi_namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ heketi_name }}
